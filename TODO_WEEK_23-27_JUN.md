# TODO — Неделя 23–27 июня

**Тема недели:** быстрые победы — мелкие P2-фичи с высоким UX-импактом. Каждая
самодостаточна, ломаться нечему. Цель: 8–10 ощутимых улучшений за неделю.

---

## 🔗 Приоритет недели — доработать клиентскую ссылку `/track/{token}`

Базовый портал уже на проде (статус, таймлайн, состав, контакт). Осталось довести
до полноценного: связать прикреплённые документы заказа с публичной ссылкой, чтобы
клиент мог скачать счёт/УПД/ТН прямо со страницы трекинга — без логина.

- [ ] **Публичная отдача файла по токену.** Новый роут
      `GET /track/{token}/file/{file_id}` (вне `login_required`):
  - проверить, что `file_id` действительно привязан к заказу с этим `public_token`
    (`AttachedFile.entity_type=='order' AND entity_id==order.id`);
  - отдавать только клиентские типы: `invoice` / `upd` / `tn` (договор/прочее — НЕ светим);
  - тот же rate-limit по IP, что и на странице трекинга.
- [ ] **Список документов на странице `track.html`.** Блок «Документы» со ссылками
      на скачивание (иконка PDF/Word, имя, тип-бейдж, размер). Показывать только если
      у заказа есть файлы клиентских типов.
- [ ] **Передать файлы в шаблон.** В `public.py/track_order`: подгрузить
      `files_for(db,'order',order.id)`, отфильтровать по client-типам, отдать в контекст.
- [ ] **Доводка UX портала** (мелочи, замеченные после первого релиза):
  - отметка времени последнего обновления статуса (когда менялся `status`);
  - аккуратный вид при отменённом/пустом заказе (сейчас отменённые → «не найдено», ок);
  - проверить мобильный вид на реальном телефоне.
- **Безопасность:** токен длинный и неугадываемый; отдаём только client-типы;
  файл строго в пределах одного заказа; внутренние документы (договор) не доступны.
- **Зачем:** снимает звонки «пришлите счёт/накладную» — клиент скачивает сам по той же ссылке.

---

## 📌 Пн 23 июня — Системные мелочи

### 1. Уведомления: удаление
- [ ] Роут `POST /notifications/{id}/delete` в `app/routers/notifications.py`
      (`@login_required`, удаляет своё/системное уведомление).
- [ ] Кнопка-крестик в дропдауне колокольчика (`base.html`) и в списке `/notifications`.
- [ ] Опционально: «Очистить все прочитанные» — `POST /notifications/clear-read`.
- **Зачем:** старые уведомления копятся, сейчас только «прочитать».

### 2. Настройки: скачать резервную копию БД
- [ ] Роут `GET /settings/backup` (`@role_required("admin")`) — отдаёт `tms.db`
      как `FileResponse` с именем `tms-backup-{ГГГГ-ММ-ДД}.db`.
- [ ] Перед отдачей — `PRAGMA wal_checkpoint(TRUNCATE)`, чтобы в файле были все данные из WAL.
- [ ] Кнопка «Скачать бэкап» в настройках (раздел «Система»/«Обслуживание»).
- **Зачем:** бэкап без доступа к серверу. ⚠️ только admin — это вся база.

---

## 📌 Вт 24 июня — Контрагенты: интеллект карточки

### 3. Проверка статуса в ЕГРЮЛ (DaData)
- [ ] Использовать уже подключённый DaData (`dadata_party` в `counterparties.py`):
      добавить в ответ `state.status` (ACTIVE / LIQUIDATING / LIQUIDATED / BANKRUPT / REORGANIZING).
- [ ] Кнопка «Проверить в ЕГРЮЛ» в карточке КА (вкладка «Общее») → AJAX →
      бейдж статуса (зелёный «Действует» / красный «Ликвидирована» / и т.д.) + дата проверки.
- [ ] Сохранять результат: новые поля КА `egrul_status`, `egrul_checked_at` (миграция).
- **Зачем:** проверка перед подписанием договора, без перехода на сайт ФНС.

### 4. «Спящий» клиент — плашка в карточке
- [ ] В `view_counterparty`: вычислить дату последнего заказа (`max(o.date)` среди не-cancelled).
- [ ] Если последний заказ был 30/60/90+ дней назад — плашка в шапке карточки:
      жёлтая «Не заказывал 45 дней» / красная «Спит 90+ дней».
- [ ] Порог брать из CompanySettings (новое поле `dormant_days`, default 45) или хардкод 30/90.
- **Зачем:** сигнал менеджеру позвонить, без захода в отчёты.

---

## 📌 Ср 25 июня — Заказы и договора

### 5. Заказы: мини-статистика клиента в карточке
- [ ] В `view_order`: посчитать по `order.counterparty` — всего заказов (не cancelled),
      общая сумма выручки, дата последнего заказа, средний чек.
- [ ] Блок «Клиент» в правой колонке карточки заказа: 4 цифры + ссылка на карточку КА.
- **Зачем:** контекст по клиенту без переключения в карточку контрагента.

### 6. Договора: дублирование («Создать похожий»)
- [ ] Роут `POST /contracts/{id}/duplicate` — копия с тем же шаблоном и КА,
      новый номер (`_next_contract_number`), даты сдвинуты на год вперёд, статус `draft`.
- [ ] Кнопка «Создать похожий» в карточке договора (по образцу заказов).
- **Зачем:** ежегодное перезаключение договоров в один клик.

---

## 📌 Чт 26 июня — Счета и логистика

### 7. Счета: комментарии и лог в карточке
- [ ] Переиспользовать инфраструктуру `Comment` / `AuditLog` (как в заказах/КА).
- [ ] В `view_invoice`: подгрузить comments + activity, передать в шаблон.
- [ ] В карточке счёта: поле комментария («почему не платит») + лента смены статуса.
- **Зачем:** фиксировать причины неоплаты прямо на счёте.

### 8. Логистика: редактирование записи
- [ ] Роут `POST /logistics/{id}/edit` (модал или инлайн) — править дату/сумму/описание.
- [ ] Кнопка-карандаш в строке таблицы логистики → модаль с предзаполненной формой.
- **Зачем:** сейчас исправить сумму можно только удалив и пересоздав запись.

---

## 📌 Пт 27 июня — Аналитика логистики + буфер

### 9. Логистика: график динамики по месяцам
- [ ] В `logistics_index`: агрегировать `LogisticsCost` по месяцам за 6–12 мес.
- [ ] CSS-гистограмма затрат по месяцам (как в дебиторке/прогнозе) над таблицей.
- **Зачем:** видно сезонные пики затрат, сейчас только 4 суммарные цифры.

### 10. Буфер
- [ ] Прогон всех 9 фич в браузере (основные сценарии).
- [ ] Коммит. Деплой — **только по явной команде**.

---

## 📊 Резюме

| День | Фичи | Тип |
|---|---|---|
| Пн | Удаление уведомлений · Бэкап БД | Система |
| Вт | ЕГРЮЛ-проверка · «Спящий» клиент | КА-интеллект |
| Ср | Мини-статистика клиента · Дублирование договора | Заказы/договора |
| Чт | Комментарии в счетах · Редактирование логистики | Счета/логистика |
| Пт | График логистики по месяцам · Буфер | Аналитика |

**Новые поля БД (миграции):** `counterparties.egrul_status`, `counterparties.egrul_checked_at`,
опц. `company_settings.dormant_days`. Новых таблиц нет — всё переиспользует существующее.

**Запасные мелочи (если останется время):** заказы — массовые действия в списке;
договора — расширенные фильтры (истекают в 30/60 дней); отчёт выручки — понедельная гистограмма.
